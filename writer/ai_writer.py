#!/usr/bin/env python3
class AI_Writer:
    def __init__(self, link: str, headline: str) -> None:
        self.link = link
        self.headline = headline

    def write_article(self) -> str:
        link = self.link
        article = ...
        return article

    def rewrite_headline(self) -> str:
        headline = self.headline
        rewritten_headline = ...
        return rewritten_headline

    def content_getter(self) -> str:
        link = self.link
        full_article_text = ...
        return full_article_text
